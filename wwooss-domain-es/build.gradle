dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-elasticsearch'
    implementation project(path: ':wwooss-common')
}

tasks.named('test') {
    useJUnitPlatform()
}
